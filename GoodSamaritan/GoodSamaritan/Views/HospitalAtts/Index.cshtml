@model IEnumerable<GoodSamaritan.Models.Smart.HospitalAtt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HospitalAttName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HospitalAttName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HospitalAttId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HospitalAttId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HospitalAttId })
        </td>
    </tr>
}

</table>

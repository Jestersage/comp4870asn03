@model IEnumerable<GoodSamaritan.Models.Client.FileStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FileStatusString)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FileStatusString)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FileStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FileStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FileStatusId })
        </td>
    </tr>
}

</table>

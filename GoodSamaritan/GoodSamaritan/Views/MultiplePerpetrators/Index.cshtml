@model IEnumerable<GoodSamaritan.Models.Smart.MultiplePerpetrator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MultiplePerpetratorValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MultiplePerpetratorValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MultiplePerpetratorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MultiplePerpetratorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MultiplePerpetratorId })
        </td>
    </tr>
}

</table>

@model IEnumerable<GoodSamaritan.Models.Client.Victim>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VictimType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VictimType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VictimId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VictimId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VictimId })
        </td>
    </tr>
}

</table>
